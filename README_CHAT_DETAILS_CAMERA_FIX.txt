Update contents:
- Clear history is now local to the requesting user only; messages remain visible to the other participant.
- Added Delete chat from my list, which clears/hides the room only for the requesting user.
- Added chat details page accessed by clicking the chat title/user name.
- Chat details page includes profile/status, media shared in the chat, chat theme controls, audio call, video call, search, clear history, and delete chat.
- Fixed reverse camera switching so the local preview refreshes immediately after switching, while the remote user also receives the updated camera track.

Files included:
- App.jsx -> replace client/src/App.jsx
- server.js -> replace your server.js
